Test project to build a java client for Github API.

TODO - instead of @all requiring the Client, just make sure that Client sets up the PAT if needed, then only the mothods that need a PAT check is the PAT set.